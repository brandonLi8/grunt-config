#====================================================================================================
#
# Copyright © 2019 Brandon Li. All rights reserved.
#
# NOTE!!: this file was generated by `grunt generate-travis`.
#
# Entry point for travis-cli.org. See undefined
#
# This file instructs travis-ci.org to check that master 'can build' using `grunt can-build` when ever master changes.
# This is to prevent rogue commits/merges on this project that aren't linted.
#
# NOTE!!: both `grunt can-build` and `grunt generate-travis` are specific to this repo's Gruntfile (Found
# https://travis-ci.org/brandonLi8/grunt-config/Gruntfile.js). Your repo's Gruntfile must extend to this Gruntfile.
#
# @author Brandon Li brandon.li820@gmail.com
#
#====================================================================================================

language:
  node_js                         # only node for now. Other templates for other languages to be added later.
node_js:
  - '10'                          # tentative version
branches:                         # build only master
  only:
    - master
before_script:
  - npm install                   # install dependencies
  - npm install -g grunt-cli      # install grunt
  - npm install grunt --save-dev
  - npm update                    # update npm dependencies
script:
  - grunt can-build               # test for a successful build
after_script:
  - echo "goodbye grunt-config, travis is done..."